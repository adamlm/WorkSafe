<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.worksafe.disruptthedistrict.worksafe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main_Screen"
            android:label="My Activity Title" />
        <activity android:name=".Submit_New_Issue" />
        <activity android:name=".Settings" />
        <activity android:name=".changeEmail" />
        <activity android:name=".changePassword" />
        <activity android:name=".about" />
        <activity android:name=".CreateNewAccount" />
        <activity android:name=".Main_Screen_Admin" />
    </application>

</manifest>